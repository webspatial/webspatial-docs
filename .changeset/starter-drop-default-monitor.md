---
"@webspatial/starter": patch
---

Remove `enable-xr-monitor` from the scaffolded Vite template and update the bundled docs. The template has no dynamic layout, so the marker was unnecessary; `enable-xr` alone is the recommended starting point.
